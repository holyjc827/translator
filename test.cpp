#include <vector>

int main()
{
    int number=0;
    printf("test");
    while(true)
    {
        if(number == 0)
        {
            break;
        }
    }
    std::vector<int, std::string> chu;
    for(int i = 0; i < chu.size(); i++)
    {
        printf("yes");
    }
    return 0;
}
